# Prime Agent home workspace instructions

This is the Git-synced mirror of `/home/prime-agent/AGENTS.md`, scoped to that workspace.
It includes the user-approved coding discipline and agent-owned upgrade checks.
Do not load these project-specific paths and commands globally in unrelated projects.
When changing the active file, update this mirror byte-for-byte in the same task,
validate, commit, push and verify the remote. The home harness has its own repository;
its local commits alone do not satisfy this repository's sync requirement.
