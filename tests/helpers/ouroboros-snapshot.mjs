// Invert only exact approved appends; unknown mutations remain visible.
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import {resolve} from 'node:path';
import {createHash} from 'node:crypto';
const sha=b=>createHash('sha256').update(b).digest('hex');
export const ouroborosTransitions={"engineering-references/references/domain-modeling.md": {"previous": "c44e31a2986803b789176b67465e5031c4846cfc2eacb8e8886b6f602d467caf", "current": "78573c0500966706693c967c8162d78aac1d48e38d3339c9ee739629afdb6ba7", "appendix": "\n## Preserve requirement authority through a relay\n\nUse this example when research, a summary or another agent supplies a specification.\nKeep origin and acceptance separate: a fact can inform a decision without becoming\none, and an agent-generated decision is not evidence of human approval. Preserve the\nspeaker/source, descriptive or proposed or accepted status, scope, conditions and\nsuperseding decision in the existing task contract; no new ledger or schema is needed.\n\nFictional example: the maintainer asks for a retrying client with an overall deadline\nbelow ten seconds. Research finds that the current client retries three times. An\nagent proposes five retries. A summary that says \u201cimplement five retries\u201d has promoted\na suggestion and dropped the deadline; neither fluency nor repetition grants authority.\nThe three statements must remain distinct:\n\n- Observation: the inspected current client retries three times, at the cited revision.\n  That describes current behavior, not the required policy for the new client.\n- Proposal: try five retries, subject to the unchanged overall deadline. It remains\n  proposed until accepted by someone authorized for that decision.\n- Accepted decision: an authorized maintainer chooses five retries while retaining\n  the below-ten-second overall deadline. Record that decision and its scope together.\n\nTrace these distinctions through research \u2192 summary \u2192 specification \u2192 implementation.\nA source citation establishes origin, not approval; a field named `user` may contain\nan automated choice. Check what the producer actually means rather than trusting the\nlabel. Keep useful observations as context, but do not promote them into hard criteria\nmerely because a requirements extractor paraphrases them. Preserve exact material\nconstraints and their qualifications even when the surrounding explanation is shortened.\n\nIf a later authorized decision changes the retry count, update the canonical task\ncontract using the existing supersession convention before implementation. Reconcile\naffected handoffs and checks; a correction only in a private summary leaves downstream\nworkers on the old contract. Do not silently replace a user requirement with an easier\nartifact or treat a test pass as acceptance of a changed requirement. Missing authority\nstays unresolved: ask the consequential question, not a full repeat interview.\n\nReview both directions: a relayed proposal must not become accepted, and a valid later\ndecision must not be discarded as mere observation. Check the resulting specification\nstill contains the count, deadline and actual acceptance status. This is a worked\nreview example, not an executed interview or permission to run an autonomous one.\nSee [source findings and limits](../OUROBOROS_SOURCES.md).\n"}, "engineering-references/references/test-design.md": {"previous": "cb97e9735779be1591c93894c6ac81b67211e11b4e160618ca4a7198ce56b61e", "current": "9a274414496a8d1d05b52a6faa80d512d700d4d6e5e31055be4a2b8d50e4a94f", "appendix": "\n## An evidence requirement must reach the approval gate\n\nUse this example when a model or adapter produces a result used for acceptance.\nA prompt saying \u201cshow evidence or fail\u201d is not enforcement if another layer defaults\nmissing evidence to success. Trace the declared requirement \u2192 response schema \u2192\nparser/defaults \u2192 stored result \u2192 final approval. Inspect the actual consumer and any\nlegacy or alternate route, not only the judge's instructions.\n\nFictional example: a checker may approve an import only after observing the required\nrow-count check for the tested input revision. Its model returns `approved: true` but\nomits the check evidence. The parser supplies an empty list for compatibility, and the\nfinal gate reads only `approved`. The response is parseable, but the required check is\nunverified. A high score or a second judge's agreement cannot supply the missing event.\n\nFor an authorized disposable fixture, state the required evidence and exercise these\ncases through parsing, persistence/reload and the real approval consumer:\n\n| Input/evidence case | Required outcome |\n|---|---|\n| Correctly typed result with supported evidence of the required check on the current revision | Eligible to pass this evidence gate; other acceptance and authorization checks still apply |\n| Missing or empty required evidence, even with `approved: true` | Unverified, not a supported pass |\n| Malformed evidence or string `\"false\"` where a boolean is required | Explicit invalid result, not truthiness-based approval |\n| A declared check was skipped or never configured | Not checked; no credit for having declared it |\n| Valid evidence shows the required row count is wrong | Supported failure, not missing evidence |\n| A legacy result parses but cannot establish required evidence | Explicit legacy/unverified state at final approval |\n\nValidate required field types and semantics at the owning boundary. Nonempty evidence\nis necessary only where the contract requires it, and is not sufficient by itself:\na citation, copied marker or unrelated successful command may not support the claim.\nBind evidence to the actual check, attempt and tested revision. Keep unknown observation\nseparate from a demonstrated product defect; do not fabricate proof to satisfy a schema.\n\nA compatibility path may preserve readable older results without granting them verified\nstatus. Ensure serialization, defaults and later aggregation preserve that distinction.\nPair each rejection with the supported positive case so a gate that rejects everything\ndoes not pass the test. Assert the stored state and final decision, not just parser\nsuccess, and never weaken the evidence requirement merely to restore green results.\n\nThese are proposed regression cases, not a new evaluator, executed model trial or\nproduction-safety claim. Use the project's existing result contract and approved test\ntools; this example adds no provider calls, dependencies or automatic grading service.\nSee [source findings and limits](../OUROBOROS_SOURCES.md).\n"}, "engineering-references/UPSTREAM.md": {"previous": "a4a5ffd65aade5fa9380783db8863671ba466a6a429844dfb109cb3a078f9627", "current": "a685474ca3410453f96ea2cc3b463b831e175c69eaf6838fbac771396fc5ac05", "appendix": "\n## Selected Ouroboros requirement and evidence examples\n\nTwo original worked examples extend [domain modeling](references/domain-modeling.md)\nand [test design](references/test-design.md): preserve requirement authority through\nrelays, and enforce required evidence through parsing and final approval. See\n[source findings and MIT notice](OUROBOROS_SOURCES.md) and ouroboros-provenance.json.\nNo Ouroboros runtime, schema, agent persona, installer, telemetry or autonomous loop\nis adopted. Earlier source identities and evidence limits remain unchanged.\n"}};
export function beforeOuroborosFile(root,relative){
 const b=readFileSync(resolve(root,relative)),r=ouroborosTransitions[relative];
 if(!r || sha(b)!==r.current)return b;
 const t=b.toString('utf8');assert(t.endsWith(r.appendix));
 const old=Buffer.from(t.slice(0,-r.appendix.length));assert.equal(sha(old),r.previous);return old;
}
