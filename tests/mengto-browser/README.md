# Owned MengTo render probe

Serve the reviewed skills tree on an owned loopback origin. Open index.html, click Run owned render checks, wait for `#probe-status[data-complete="true"]`, and inspect its actual booleans and pixels. Repeat with reduced motion. Uses real Canvas2D pixel changes/stability and native controllers; it does not prove OS-hidden, GPU performance or all input modes. Generic browser completion is not probe success.

Surface fixture: open surfaces.html, click Check scoped material CSS and inspect `#surface-result`. It loads the actual library after sampling the neutral host and checks computed styles, state and narrow overflow. Forced colors and 200% text zoom are not exercised here. Resource-release observations in the canvas probe use host diagnostics; pixel movement/stability is independently sampled.
