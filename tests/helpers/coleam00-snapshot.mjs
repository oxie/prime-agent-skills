// Exact approved append inversions; unknown content keeps failing historical hashes.
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import {resolve} from 'node:path';
import {createHash} from 'node:crypto';
const sha=b=>createHash('sha256').update(b).digest('hex');
export const coleam00Transitions={"engineering-references/references/agent-evaluation.md": {"previous": "443464f6c7706c0b104f5d59b048a9783eefc22e59ad1096d4374e3f132480f5", "current": "0c9f22ca83b46675718652e228a0a4b0b2b6574675e4f65134b9e03d418b7e34", "appendix": "\n## Comparing instructions without confusing the intervention with the outcome\n\nUse this only within an already authorized comparison and its fixed call/spend budget.\nTurn the selected instruction into an observable claim and record whether each case\nactually exercises it. Keep `not applicable`, `complied`, `violated` and\n`unobservable/error` separate; a task that never reaches a rule supplies no evidence\nfor removing it. Observe resulting state when a diff cannot establish the claim.\n\nRecord the treatment baseline after the approved instruction change, separately from\nthe starting code revision. Measure task output against that baseline: deleting a\nrules file is the intervention, not implementation progress. A no-op in either arm\nmust remain a no-op; if the case legitimately requires no change, judge its intended\noutcome rather than require a nonempty diff. Before blind grading, remove treatment-only\ndeltas and arm labels, assign opaque IDs and keep the mapping outside grader input.\nRetain the raw artifacts for authorized traceability. If remaining content reveals\nthe arm, report limited blinding rather than claim an unbiased comparison.\n\nSeparate new-file work from edits with nearby examples. An agent can copy an existing\nconvention without its written rule; agreement on that edit does not show that the\nrule is redundant for new modules. For example, both arms might register an endpoint\nbeside a similar route while only one registers a new worker in a separate registry.\nGrade the actual registration contract, not code resemblance, and report the contexts\nseparately. Neither a few equal outcomes nor an inapplicable case justifies deleting\na security, approval or release requirement. Any removal needs its own authorization\nand evidence appropriate to that requirement; do not spend more calls to force a\npreferred conclusion.\n\nUse the existing trial ledger: preserve failed starts, nonzero exits, unknown costs\nand incomplete outcomes, even when a diff or response looks useful. Worktrees isolate\ncheckout edits, not credentials, network effects, shared Git state or provider spend.\nThese are evaluation-design checks, not an ablation runner or permission for automatic\ninstruction pruning. See [selection and limits](../COLEAM00_SOURCES.md).\n"}, "engineering-references/references/test-design.md": {"previous": "f9dd35e1ae25e6c089c2b8a1c723b87463bed73806688c42a208e1a30bc143e4", "current": "23a23ea1edfcee93bd639e3544ac5f305d7e52da9cd4c972626481e3499c8a6a", "appendix": "\n## Validate the detector, including its evidence path\n\nWhen an authorized mutation or negative fixture is meant to prove detection, first\nrun an unchanged positive control in the same disposable environment. Include the\nsame required config, dependencies and evaluator revision. If that control fails,\nreport broken setup; no mutant in that environment earns detection credit. Verify\nthat the intended mutation was actually applied. A missing anchor is not a caught\ndefect, and a timeout is not the intended assertion failure.\n\nFictional example: a refund fixture must reject an amount above the approved limit.\nThe unchanged copy passes. Removing the limit check should fail the named\n`rejects-over-limit` assertion. If the copied project instead lacks a required config\nfile and fails at startup, classify it as infrastructure failure, not a caught refund\nbug. If another assertion fails, record what it detected but do not credit the claim\nthat `rejects-over-limit` protects this behavior. Require the completed process result\nand the specifically expected failure evidence together; a printed success marker\nfollowed by a nonzero exit is not a passing control.\n\nFollow the entire observation path, not only the expected-value expression. A hidden\nassertion that reads through a candidate-editable adapter can observe fabricated\nsuccess. Identify who can read or write evaluator entrypoints, adapters, config,\ninputs, process-status capture and result artifacts. Keep the trusted evaluator and\nevidence channel outside candidate control using the project's approved isolation;\nif the candidate legitimately changes an adapter, review and validate that boundary\nseparately rather than silently trusting it as the oracle. Protecting one file or\nstarting a fresh model context does not establish filesystem/process isolation.\n\nThis is a proposed test recipe, not an executed refund test, new harness or authority\nto run untrusted audit scripts. Static presence checks and deterministic execution\nare not security certification. Preserve independent expected outcomes and existing\nreal-boundary coverage. See [selection and limits](../COLEAM00_SOURCES.md).\n"}, "engineering-references/references/api-authorization.md": {"previous": "62b12ba0aeb5e791d259e7e1893a95697e14c954a652313e339107ac8dbb387a", "current": "5469fb84fd1b414ec4ca0a68529428da2c739d12aa20bbdd9e219c8032071f7c", "appendix": "\n## A hold must survive the next entrypoint\n\nUse this example when a protected action can be reached through approval, retry,\nqueue dispatch or an administrative path. Successful validation and permission to act\nare separate facts. Enforce the current policy at the final effect boundary, not\nonly in the caller that initially records a hold. Keep the hold durable and bound to\nthe intended resource, operation and revision; a generic `passed` status must not\nsilently erase it. Missing, stale or unreadable approval evidence cannot authorize\nthe protected action. An explicit administrative exception still needs its own policy.\n\nFictional example: a document export for revision r7 passes validation but awaits a\nreviewer's approval. The first request returns `held`. A retry, the next queue tick\nand a direct supported export endpoint must all leave the protected export and its\nqueued delivery unchanged. A test of the first response alone misses a dispatcher\nthat treats `passed` as permission. Use the actual shared state and final boundary\nin a disposable fixture; a mocked caller refusal proves only that caller's behavior.\n\nInclude an authorized positive case: an allowed reviewer releases the r7 hold and\nexport proceeds under the existing duplicate-effect policy. If the document becomes\nr8 before execution, r7 approval cannot authorize r8; require the policy's reapproval\nor refusal. Check revision and permission atomically with the effect, or use the\nservice's equivalent expected-revision precondition, so a concurrent change cannot\nslip between check and action. Repeat the denied cases across relevant entrypoints\nand assert both stored state and forbidden delivery/outbox effects, while allowing\nexplicitly permitted security logging.\n\nThese are proposed regression cases, not proof of a deployed approval system. Use\nexisting authorization/state mechanisms; do not introduce a scheduler, approval\nservice or automatic merge path. See [selection and limits](../COLEAM00_SOURCES.md).\n"}, "engineering-references/UPSTREAM.md": {"previous": "2156478cbd3ffc38df8f7323de312143bded2eba814c0b55f389d2d43876b0d1", "current": "40b65058ac08813aae6c7c31d5d1ef0a66115d4b4ad57be1a4013768e7bc5019", "appendix": "\n## Selected Coleam00 evaluation and approval examples\n\nThree original additions extend existing agent-evaluation, test-design and API\nauthorization references: instruction-comparison confounds, validator controls and\nevidence-path integrity, and revision-bound holds across later entrypoints. See\n[selection and MIT notice](COLEAM00_SOURCES.md) and coleam00-provenance.json.\nNo upstream runner, hook, desktop tool, ablation experiment, new skill or memory\nsystem is installed. Historical source identities and verification limits remain\nunchanged; these examples do not establish model effectiveness or production safety.\n"}};
export function beforeColeam00File(root,relative){
 const b=readFileSync(resolve(root,relative)),r=coleam00Transitions[relative];
 if(!r || sha(b)!==r.current)return b;
 const t=b.toString('utf8');assert(t.endsWith(r.appendix));
 const old=Buffer.from(t.slice(0,-r.appendix.length));assert.equal(sha(old),r.previous);return old;
}
